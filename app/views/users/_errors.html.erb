<% if @user.errors.any? %>
    <p class="alert alert-danger" role="alert">The form has <%= pluralize(@user.errors.count, 'error') %>.</p>
    <ul>
	<% @user.errors.full_messages.each do |message| %>
	    <li><%= message %></li>
	<% end %>
    </ul>
<% end %>
